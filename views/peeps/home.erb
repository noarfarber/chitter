<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Homepage</title>
</head>
<body>
  <% if @user %>
  <h1>Hello, <%= @user.username %> </h1>
<% end %>

  <h2>Welcome to Chitter</h2>
  <ul>
    <% @peeps.each do |peep| %>
      <li>
        <%= peep.message %>
        <%= peep.created_at[0...16] %>
        <%= peep.user_id %>
      </li>
    <% end %>
  </ul>
    <form action="/users/<%= @user.id %>/peeps/new">
      <input type="submit" value="Post a Peep">
    </form>
</body>
</html>
